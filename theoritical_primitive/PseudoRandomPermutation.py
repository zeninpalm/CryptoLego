'''
Pseudo-random Permutation
'''
import PseudoRandomFunction

class PseudoRandomPermutation(PseudoRandomFunction):
    def __init__(self, implementation):
        pass

    def map(self, key, plain_text):
        '''
        Require:    Key_given_and_legal:   (key != None) and is_legal_key(key)
                    Plain_text_given_and_legal:     (plain_text != None) and is_legal_plain_text(plain_text)
        Ensure:     len(Result) == len(plain_text)
        '''
        pass
